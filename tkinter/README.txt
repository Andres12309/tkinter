1) importa la libreria tkinter luego creamos una ventana raiz con la clase Tk y creamos un label que contentra el texto hello tkinter.

2)importa la libreria tkinter luego declara un ventana Tk y dicha ventana Tk llama a una ventana de mensaje msg que imprimira una texto,
el texto se almaceno en una variable llamada whatever_you_do y dicha ventana de mensaje tendra un fondo(background),
ademas una fuente (italic) y tamaño de letra 24.

3)crea una ventana raiz en el cual se define una clase en el cual contendra dos funciones uno que crea un widget que tiene sus prpias caracteristicas
 y controlara los dos botones, y la otra funcion que contendra el texto que se imprimira en consola.

4)crea una ventana raiz en donde se declara una variable de texto que se enlaza con valores enteros y se crea un label
para el texto, ademas creadoo dos widgets de entrada cada uno tiene su prpio texto para su seleccion.

5)se hace una lista  de cada lenguaje con su sufijo que es un numero y cuando se da click en cada uno de sus 
widgets de seleccion de entrada que muestra un label con texto en la ventana raiz que se actualiza al cambiar de seleccion y ademas el sufijo en la consola.

6)muestra widgets de entrada que al hacer click impime un label de texto que se actualiza al presionar otro boton y ademas imprime su sufijo en la consola, similar al 6.

7)en una ventana raiz muestra 2 widgets de chequeo o eleccion con el label de texto con genero.

8)en una ventana matriz muestra 4 widgets 2 de eleccion y 2 de accion que, existe una funcion para imprimir el texto al precionar el segundo boton que es comprado con
los widgets de eleccion para saber si es 1 sie hay elcceion o 0 sin no hay eleccion y los imprime en consola, ademas el el segundo boton de accion para finalizar.

9)una ventana raiz que muestra dos label con la descripcion solicitada y dos grid para ingresar textos o datos solicitados.

10)una ventana raiz que muestra dos label con la descripcion solicitada y dos grid para ingresar textos o datos solicitados, ademas agraga dos widget de accion,
 que imprime en consola los datos puestos en el grid y el otro boton para finalizar.(8 y 9 unidos)
